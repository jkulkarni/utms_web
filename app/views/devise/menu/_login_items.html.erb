<% if user_signed_in? %>
  <%= link_to 'Logout', destroy_user_session_path, :method => :delete, class: ' ctrans-buttons__btn btn btn-default' %>        
<% else %>
  <%= link_to 'Login', new_user_session_path, class: ' ctrans-buttons__btn btn btn-default'  %>  
<% end %>

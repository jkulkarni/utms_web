<% if user_signed_in? %>
  <%= link_to 'Change password', edit_user_registration_path, class: 'ctrans-buttons__btn btn btn-default' %>
<% else %>
  <%= link_to 'Register', new_user_registration_path, class: 'ctrans-buttons__btn btn btn-default' %>
<% end %>